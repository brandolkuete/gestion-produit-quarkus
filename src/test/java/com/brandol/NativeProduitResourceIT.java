package com.brandol;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeProduitResourceIT extends ProduitResourceTest {

    // Execute the same tests but in native mode.
}